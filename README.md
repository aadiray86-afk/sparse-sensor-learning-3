# sparse-sensor-learning-3
Simulates an IEEE 33-bus solar-integrated feeder to test whether a graph-aware model localizes faulted sections better than non-graph baselines when sensors are sparse, noisy, and dropping data — then checks if those predictions support safe, load-restoring switch decisions.  (297 characters.)
